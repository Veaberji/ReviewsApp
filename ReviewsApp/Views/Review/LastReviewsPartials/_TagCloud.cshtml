@model IEnumerable<TagCloudViewModel>

<div>
    @foreach (var tag in Model)
    {
        <a class="@tag.CssClass"
       asp-action="ReviewsWithTag"
       asp-route-tagText="@tag.Text"
       asp-route-pageIndex="1">
            @tag.Text
        </a>
    }
</div>
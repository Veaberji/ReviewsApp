@model IEnumerable<string>

<p class="text-start">
    @foreach (var tag in Model)
    {
        <a class="fs-5"
       asp-controller="Review"
       asp-action="ReviewsWithTag"
       asp-route-tagText="@tag">@tag</a>
    }
</p>